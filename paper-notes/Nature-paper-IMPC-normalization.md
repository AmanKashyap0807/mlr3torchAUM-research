https://www.nature.com/articles/s41598-024-61365-z

IMCP makes sure every class counts the same when building the curve. Instead of letting big classes dominate just because they have more samples, the method gives each class an equal total weight; within a class, that weight is split evenly across its samples.

step for normalization :
- per sample widths
- per sample quality
- building the curve